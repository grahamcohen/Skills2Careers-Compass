# Skills2Careers Compass

A static web prototype linking labour market intelligence to skills in demand across the East African Community.

## What it covers

- **8 countries**: Kenya, Tanzania, Uganda, Rwanda, Burundi, South Sudan, DR Congo, Somalia
- **3 sectors**: Agritech, Renewable Energy, Digital Economy
- **5 audiences**: Students/Graduates, Career Specialists, Educators, Employers, Policymakers (tailored briefings via the Compass Users drawer)

Built on the UNESCO Global Skills Tracker and the ESCO occupation taxonomy. See the in-app **About** drawer (info icon, top-left) for the full data-source list.

## Running locally

It's a static site — no build step. Open `index.html` directly in any modern browser, or serve it for proper relative-path behaviour:

```bash
# Python (works without extra installs)
python3 -m http.server 8080

# or via npm script
npm run serve
```

Then visit <http://localhost:8080>.

### Optional: linters and validators

Linter configs ship in the repo but require their npm packages:

```bash
npm install            # installs dev dependencies
npm run lint           # JS + HTML + CSS lint
npm run format         # Prettier across the codebase
npm run validate:data  # JSON schema sanity checks
npm run validate:links # HTTP-checks every link in the resource files
npm test               # smoke tests (JSON loads, key data shapes)
```

## Repository layout

```
index.html          single-page entry, all UI shell + drawers + modals
app.js              ~9k lines of UI logic (split out per-concern is a
                    standing refactor item — see the original AUDIT.md)
data.js             ~2k lines of hardcoded reference constants
manifest.json       PWA manifest (theme colour, icons, scope)
style.css           ~50 lines of custom CSS layered over Tailwind CDN

courses.json        training providers and outcome scorecards
top_occupations.json
top_skills.json
ventures.json
scholarships.json
sector_data.json
app_data.json       narrative copy
app_config.json     toggles
wages.json          simulated wage data — see Prototype notice in app
resources_general.json
resources_evidence.json
resources_digital.json
resources_agri.json
resources_energy.json
providers_tracer_studies.json  evidence data for future wire-up

simulate_wages.py            regenerates wages.json
simulate_employers.py        regenerates an employer mapping
validate_data.py             top-level JSON schema sanity check
validate_links.py            HTTP-status check on every link in the JSONs
export_analysis_report.py    pull together a summary report

tests/                       Node smoke tests (no framework — just asserts)
```

## Status of major features

- **Career Pathway Builder** (homepage → Skills Match): works
- **Find Courses** (table + mobile cards, full filters): works
- **Sector Hub** (Agritech / Renewable Energy / Digital): works
- **Occupation profiles**: works, with skill chips and similar-roles cross-links
- **Skill profiles**: works, with job-title chips that link back to occupations
- **Careers Hub** (with CV builder, AI Interview Coach, Application Kit, Mentors & Alumni, Founders Launchpad, Financial Aid): works; nested sub-views have Back-to-Careers-Hub buttons
- **My Career Plan** (bookmark widget): works, persists to localStorage
- **About drawer** (info icon, top-left): works
- **PWA install**: manifest loads but installability requires real PNG icons (see About > Prototype notice)
- **Language switcher**: EN works in full; SW/FR show a "coming soon" toast and snap back to EN
- **Service worker / offline**: not registered — single-file prototype only

## A note on the data

This is a **working prototype**. The interface design and navigation flows are intended to be production-quality, but a portion of the data is **illustrative rather than sourced**:

- Wage figures (`wages.json`) are generated by `simulate_wages.py`
- AI Interview Coach feedback is randomised mock text
- Some outcome-scorecard percentages on training cards come from a default profile when a real one isn't available
- Mentor avatars on the `specificMentors` block in `data.js` (currently unused in the UI) point at `i.pravatar.cc` placeholder portraits

Anywhere the user sees a small amber **Demo** pill or hovers a tooltip pointing to the About drawer, they're looking at this illustrative content. Course providers, training programmes, ventures, scholarships, and resource links are all real and verified.

## Contributing

The canonical upstream is **carmelasalzano-source/Skills2Careers-Compass**. This fork (grahamcohen/Skills2Careers-Compass) tracks an audit-and-fixes branch with the corrections documented in `CHANGES.md`. Pull requests go upstream.

For any code edits, please keep:

- Conventional Commits style (`fix:`, `feat:`, `chore:`, with a scope where useful)
- `node --check app.js` clean (the syntax check before each commit)
- LF line endings in the repo (the `.gitattributes` handles platform-native checkout, so Windows working copies will still see CRLF — let Git do its job)
- The Prototype notice in `index.html` and the About drawer in sync if you change demo-data status

## Licence

MIT (per `package.json`).
